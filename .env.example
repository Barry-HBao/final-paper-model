# Example environment variables
# Force CPU device (set to 1 to force CPU even if CUDA present)
FORCE_CPU=0
# Change tokenizer/model max length if needed
MAX_LENGTH=128
# VADER thresholds
VADER_POS_THRESHOLD=0.05
VADER_NEG_THRESHOLD=-0.05
